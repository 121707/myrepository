server:
  port: 3300
spring:
  application:
    name: config-center
  cloud:
    config:
      server:
        git:
          uri: git@github.com:121707/myrepository.git
          search-paths:
            - myrepository
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7081/eureka/

